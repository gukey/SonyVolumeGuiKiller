# 1.0.4 真机验证

日期：2026-09-06。设备：Sony BRAVIA 4K AE2，Android 14 / API 34。

电视原版为 1.0.3 / versionCode 4，因此修复版使用 1.0.4 / versionCode 5。
因签名不同，先备份旧 APK、应用 files 和无障碍服务列表，再重新安装并恢复。
恢复前后两个 ADB 授权文件 SHA-256 一致；备份、密钥及设备日志均未提交仓库。

| 验证项 | 结果 |
| --- | --- |
| Gradle 构建、6 项单元测试 | 通过 |
| lintDebug | 0 错误，6 条非阻断警告 |
| 安装与服务绑定 | 1.0.4 正常安装；守护和 Projectivy 均已绑定，Crashed services 为空 |
| 实际拦截 | dumpsys audio 显示 mVolumeController=VolumeController(null,mVisible=false)；页面报告成功 |
| 连续音量键 | 注入 10 组减音量/加音量，控制器仍为空；测试结束将音量还原至测试前的 5 |
| 授权文件临时不可用 | 临时改名应用私有 key 后报告失败，恢复文件后同一进程自动重新成功，无需重启 |
| 服务重新绑定 | 仅移除并恢复本应用的无障碍条目，服务重新连接并成功；Projectivy 配置保留 |
| 短暂待机唤醒 | 睡眠 3 秒后唤醒，电视状态为 Awake，守护再次成功 |
| 系统恢复任务 | 持久化周期任务 1701 已注册；强制执行任务成功触发已连接服务检查 |
| 退出应用页面 | 返回桌面后成功时间继续更新，控制器保持为空 |

截图中的 PC 客户端曾报告无障碍 disabled，但系统 enabled_accessibility_services 和 Bound services 均显示已启用、已绑定，属于 PC 诊断误判。本次没有修改 PC 客户端。

这是短时功能与故障恢复验证，不等于长时间稳定性结论。尚未验证多日运行、长时间深度待机、整机重启、真实 adbd 断连或系统强制杀进程；没有强行重启电视或停止 adbd。

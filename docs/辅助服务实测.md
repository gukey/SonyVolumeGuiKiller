# 自动 5555 0.3.5-sony.1 实测记录

日期：2026-09-06。设备：Sony BRAVIA 4K AE2，Android 14。配套音量守护：1.0.4。

| 检查 | 结果 |
| --- | --- |
| APK 编译、单元测试、lint | 构建成功，8 项测试通过，0 错误、31 条警告 |
| PC 1.0.5 打包与窗口 | 发布成功，主窗口和帮助窗口加载通过，内嵌 APK SHA256 与单独 APK 一致 |
| 授权迁移 | 旧 APK 和私有数据已备份，三个授权文件恢复后 SHA256 一致；备份不上传 |
| 正常运行 | 持续输出 `guard health=true`，真实 ADB shell 回显验证成功 |
| 辅助进程异常退出 | 仅终止辅助应用进程，PID 从 23112 变成 23346；8 秒后的检查已恢复健康。未手动打开应用恢复 |
| 5555 中断 | 将 adbd 临时切到 5566；03:53:49 检测到 5555 拒绝连接，自动发现无线调试端口，03:54:08 恢复 5555 并验证成功。从发现故障到恢复约 19 秒 |
| 短时待机唤醒 | 发送休眠键，15 秒后唤醒；ADB 命令检查正常，音量控制器为 `null` |
| 关闭网页管理 | 在电视界面关闭开关，9093 网页不可访问；17 秒后同一辅助进程仍输出健康检查成功，再打开开关网页恢复。测试后已恢复开关 |

上述为本次单轮短时实测，不是 24/48 小时耐久测试，不等同于完全断电开机或长时间深度待机。没有执行整台电视重启。系统强行停止、撤销权限和固件限制仍可能阻止应用恢复。

状态字段 `guardHealthy` 表示近期命令检查结果，检查间隔内可能保留上次结果；结合 `adb5555Available` 和 `lastGuardCheck` 判断。通过 PC 导入授权文件后，网页 `isPaired` 标记可能仍为 false，不能据此否定已经成功的 ADB 命令验证。

APK SHA256：`820644de7b7745c81abf8dab284e36e973643344a52d9089cc0949c38484371a`。

本机新版 PC 工具位于 `exe/1.0.5`；正在运行的旧 EXE 未关闭。旧辅助 APK 归档至 `apk/历史版本`。
